&НаСервере
Функция Загрузи_НаСервере(КонтрагентСсылка)
	ЭтаОбр = РеквизитФормыВЗначение("Объект");
	Результат = ЭтаОбр.ЗагрузиГУИДЫ_ДляЮЛ_ПоИННКПП_НаСервере(КонтрагентСсылка);
	Возврат Результат;
КонецФункции

&НаКлиенте
Процедура Загрузи(Команда)
	//Интерактивный вызов обработки, не из модуля формы объекта - элемента справочника
	Результат = Загрузи_НаСервере(ЭтаФорма.Контрагент);
	Сообщить("Результат = "+Результат);
КонецПроцедуры
