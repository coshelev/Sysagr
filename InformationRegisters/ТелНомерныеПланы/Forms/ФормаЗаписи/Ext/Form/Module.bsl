&НаКлиенте
Процедура НачДиапазонаПриИзменении(Элемент)

// Если введено значение начального номера диапазона, то постараемся расчитать значения для
// конечного номера диапазона путем добавления символов "9" вместо символов "0"
//-------------------------------------------------------------------------------------------------
	Если (Запись.НачДиапазона > 0) Тогда
	//	Запись.КонДиапазона = ПреобразованиеЗначений.ПолучитьКонецДиапазона(Запись.НачДиапазона);
	КонецЕсли;
КонецПроцедуры
